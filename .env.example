# Optional. If unset, the app uses deterministic heuristic fallbacks.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Optional. Raises Semantic Scholar rate limits when provided.
SEMANTIC_SCHOLAR_API_KEY=
